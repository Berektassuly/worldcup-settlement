# App
NEXT_PUBLIC_APP_NAME="WorldCup Settlement"

# TxLINE public API root. Keep this value without a trailing slash.
TXLINE_BASE_URL="https://txline.txodds.com"
TXLINE_NETWORK="mainnet"

# Server-side data credentials created through the TxLINE free tier activation flow.
TXLINE_JWT=""
TXLINE_API_TOKEN=""
TXLINE_FIXTURE_LIMIT="8"
